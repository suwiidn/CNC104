// โครงสร้างควบคุมพื้นฐาน
Sequence คำสั่งเเบบลำดับ
Condition คำสั่งเงื่อนไข
Loop คำสั่งทำซ้ำ

windowe check version python : python --version

// Input from user

variable = input("comment :") // Variables will always be strings.

/if you want variable be Integer.

 variable = int(input("comments :"))



// Output 

print("String" , "String") // when using String. 
print(Integer,Float) // when need to pluss or math for integer or float. don't have ("") .

// Comment

#  using for a line or comment back # .

'''     comment     '''  using when you want comment many line, you can use (' or ")!!!.

'''  comment
      comment '''


// Data Type

Primitive data type 

numbers :int , float , complex(เชิงซ้อน) 
logic : bool -- True and False
String
NoneType != 0

// Variable 
No need to declare the type.

  name = "suwijuk"
  age = 19
and 
  name,age = "suwijuk",19

/ Start variable name with only A-Z or a-z or _

/ How to check variable type.
  print(type(name))
  print(type(age))
 

// Casting

int(variable data)
float(variable data)
str(variable data)

!!! print("",int(variable)) # Casting only this line.


// Operators

Operator = ตัวดำเนินการ

 + , - ,* ,/ ,%

 ** = expo
 // = หารไม่เอาเศษ

Operand = ตัวถูกดำเนินการ


// IF Statement

/ only if
  if condition:

/ 
  if condition :

  elif condition :

  else :

/ pass  = This is a command that indicates no action will be taken; the program needs to run first without any specific instructions.

  if num < 50 :
    pass
  else :
    pass

 // ternary Operator (การเขียนif else เเบบย่อ) 

      statement if condition else statement
      ex. print("yes") if num%2 ==0 else print("No")

// Nested-IF (เงื่อนไขซ้อนเงื่อนไข)


// match service (case)
 it same case in C.

 match variable:
     case condition variable 1 :
        statement
     case condition variable 2 :
        statement
     case condition variable 3 :
        statement      


// Loop

  /for Loop
 
   for variable in range(Default number , Number of rounds ,เพิ่มที่ละ ):
     statement


  /while Loop !!! careful loop infinity.
 
   while condition:
     stetment

   while true: #infinity loop
           condition
           if condition :
             break  
  

  //break = Exit loop 
  //continue = Back to start a new loop

//Nested-Loop (ลูปซ้อนลูป)



//String function 

.upper() = ทำให้เป็นพิมพ์ใหญ่ทั้งหมด
.lower() = ทำให้เป็นพิมพ์เล็กทั้งหมด
.strip() = ลบช่องว่างซ้ายขวา
.split(String) = เเบ่งข้อความตามString 


len(String) = วัดความยาวของตัวอักษรรวมช่องว่าง strt 0 to n-1
startswit(String) = เช็กว่าเป็นตัวเริ่มต้นจริงหรือมั้ย output : boolean
endswith(String) = เช็กว่าเป็นตัวลงท้ายจริงหรือมั้ย output : boolean
find(String) = หาตัวที่ต้องการ  output : position 
count(String) = นับอักษรที่ซ้ำกัน
replace(old , String) = ลบตัวเก่าเพิ่มตัวใหม่


// List

rang(strat , stop , stap) = สร้างลำดับตัวเลขstart 0 
Ex suwijuk = list(rang(10 , 20 ,3))
output = suwijuk =[10,12,14,16,18,20]


การบวก list 
a = [1,2,3]
b = [4,5,2]
print(a+b) 
output : {1,2,3,,4,5,2}



Function in list 
len(list) = จำนวนในลิส
min(list) = จำนวนที่น้อยที่สุดในlist
max(list) = จำนวนที่มากที่สุดในlist
sum(list) = บวกกันทุกจำนวนในlist

list[start : end ] = การแบ่งส่วนช่วยให้เราสามารถแยกส่วนใดส่วนหนึ่งของรายการออกมาได้

list.append(value) = เมธอด append() จะเพิ่มองค์ประกอบใหม่เข้าไปที่ท้ายรายการ
Ex name = ["dan" , "bam" , "j" ] 
   name.append("mix") 
   print(name) 
output [dan , bam , j , mix] 

list.insert(index , value) = เมธอด insert() จะเพิ่มองค์ประกอบใหม่ที่ดัชนีที่ระบุ(กำหนดต่ำเเหน่ง)

list.pop(index) = remove list[index] 
list.pop() = if no index  will removes last position.

list.index(value)  = find position in list 

list.sort() = เรียงเลขจากน้อยไปมาก 
list.reverse() = เรียงเลขจากมากไปน้อย

get(key , default) =   ส่งคืนค่าของคีย์
*name = {"suwuijuk" : 19 , "baramee" : 20 }

keys()  : 
for keys in  name.keys() :
   print(keys)
output =  suwuijuk \n baramee

values() :
for values in  name.values() :
   print(valuse)
output =  

items() :
for keys//"suwijuk" , values in name.item() :
   print(keys)
   print(values)






//numpy 
single data type
NumPy (Numerical Python) is a library for numerical computing
import numpy as np

syntax
variable  = np.array([, , , , , ,])        //varaable = [ , , , ,,  , , ,] this for list. not numpy 

การบวก np
a = np.array([1,2,3])
b = np.array([1,2,3])
print(a+b)
output :[2,4,6]

Common Patterns
varable = np.array([...]) → from list
varable = np.zeros(n) → all zeros
varable = np.ones(n) → all ones
varable = np.arange(start, stop, step) → sequence

2มิติ :
a = np.array([[1,2,3,4,]
             ,[4,5,6]])

dtype :
a = np.array([1,2,3,4])
print(a.dtype)
or  make np.array type :
a = np.array([1,2,4],  dtype = float)

shape : Shape describes the size of an array
print(a.shape)
output : size array a

ndim :  เช็คมิติ Dimensions check 
print(a.ndim) 
output : dimensions array a 

reshape : การเปลี่ยนจากมิติ
a = np.array([1,2,3,4,5,6])
b = a.reshape(2 //row,3 //col)
print(b)
output : [[1,2,3]
          [4,5,6]]

np.sum(variable) 
np.mean(variable)
np.max(variable)
np.min(variable)

axis : 
axis=1 → sum by row
axis=0 → sum by column 
print(a.sum(axis =1 )) //บวกกันเเต่ละเเถว




//pandas 
สามารถทำงานกับข้อมูลในรูปแบบตาราง แถวและคอลัมน์

syntax :

data = { "name" : [suwijuk ,doramon , pikapika]
       "age"  : [19,4,5]}
df = pd.DataFrame(data)

data = [ {"name" :"suwijuk " , "age" : "19" } ,
       {"name" :"dandann " , "age" : "20" }]
df = pd.DataFrame(data)       

for list to pandas lists
a =  [["suwijuk" , 19] , ["dandan" , 20]] 
df =  pd.DataFrame(a , columns = ["name " ,"score"])



.head(n) :ดึงข้อมูลมาให้ดูแค่  n  บรรทัดแรก
pirnt(df.head(n))
.tail(n) : ดึงข้อมูลมาให้ดูแค่  n  บรรทัดสุดท้าย
pirnt(df.tail(n))

.info() : เอกซเรย์โครงสร้างข้อมูล
Data Type: คอลัมน์นี้เป็นตัวเลข (int64, float64) หรือเป็นข้อความ (object) ถ้าข้อมูลอายุแต่ดันโชว์เป็น object แปลว่าเราต้องหาทางแปลงค่าแล้ว
Missing Values: ตรงที่เขียนว่า 3 non-null หมายความว่ามีข้อมูลที่ "ไม่ว่างเปล่า" อยู่ 3 ตัว ถ้าสมมติว่าตารางเรามี 10 แถว แต่คอลัมน์คะแนนบอกว่ามี 8 non-null แปลว่ามีเด็ก 2 คนที่คะแนนหายไป (เป็นค่า Null)
print(df.info)


.shape : find size  วัดขนาดตาราง
print(df.shape)


Selecting Data :  df["name"]  or  df[["name", "score"]]  
data = [ {"name" :"suwijuk " , "age" : "19" } ,
       {"name" :"dandann " , "age" : "20" }]
data = pd.DataFrame(data)       
data = data["name"]
print(data)
output : จะเเสดงเเค่ list ของ name 



.iloc : กำหนดส่วนที่ต้องการจาก row and col 

df.iloc[0:4][:]




Filtering Data (Condition) :

df[df["score" > 80 ]] // เเบบฟิวเตอเดียว
df[df[("score" > 80) & ("name" == "suwijuk") ]]  //เเบบหลายฟิวเตอร์ (ต้องใช้ | & เเทน or and เท่านั้น)




Sorting Data : การเรียงลำดับข้อมูล 

df_sorted = df.sort_values("score") // เรียงตามคอลัมน์ score จากน้อยไปมาก (ค่าเริ่มต้น)
df_sorted_desc = df.sort_values("score", ascending=False) // เรียงจากมากไปน้อย (Descending)
top_3 = df.sort_values("score", ascending=False).head(3) //ดึง Top 3 ที่ได้คะแนนสูงสุด
df_multi = df.sort_values(["score", "name"]) //เรียงตามหลายคอลัมน์ (เรียง score ก่อน ถ้าเท่ากันค่อยเรียง name)




การจัดกลุ่มข้อมูล (GroupBy / Aggregation)  .groupby :

df.groupby("name")["score"].mean() // จัดกลุ่มตามชื่อแล้วหาค่าเฉลี่ยคะแนน

.agg()

df_group.groupby("name")["score"].agg(["mean", "max", "count"]) // จัดกลุ่มและคำนวณหลายค่าพร้อมกัน (ค่าเฉลี่ย, ค่าสูงสุด, จำนวน)




pd.merge() :การเชื่อมข้อมูลหลายชุด
df1 = pd.DataFrame({
    "id": [1, 2],
    "name": ["Alice", "Bob"]
})
df2 = pd.DataFrame({
    "id": [1, 2],
    "score": [85, 90]
})
df_merged = pd.merge(df1, df2, on="id") 
print(df_merged)
output : จะได้ข้อมูลscore เข้ามาเพิ่ม



การอ่านและเขียนไฟล์ CSV (Read & Write CSV) :

input file 
df = pd.read_csv("data.csv")
print(df.head())

output file :
df.to_csv("output.csv", index=False)